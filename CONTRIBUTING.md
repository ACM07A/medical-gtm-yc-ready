# Contributing

Use small, verifiable changes.

Before opening a PR:

```bash
npm run lint
npm run test
npm run demo
npm run smoke
```

Do not commit databases, secrets, real patient records, generated credentials, browser profiles or real medical documents. Keep all demo data synthetic.
